[project]
name = "clearing"
version = "1.0.0"
description = "A Flet-based web application to calculate and clear shared expenses fetched from Google Spreadsheets"
requires-python = ">=3.11"
dependencies = [
    "flet[web]>=0.85.3",
    "gspread>=6.2.1",
    "pandas>=3.0.3",
]
